<Window x:Class="Lakbay.AgentDesktop.BookingConfirmationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Confirm Booking" Height="320" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource BackgroundBrush}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Item.Name}" FontSize="16" FontWeight="Bold" TextWrapping="Wrap" />
        <TextBlock Grid.Row="1" Text="{Binding Item.PriceDisplay}" Foreground="Gray" Margin="0,4,0,16" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <TextBlock Text="Travel date:" Width="100" VerticalAlignment="Center" />
            <TextBlock Text="{Binding DateSlot}" FontWeight="Bold" VerticalAlignment="Center" />
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="Customer id:" Width="100" VerticalAlignment="Center" />
            <TextBox Text="{Binding CustomerId, UpdateSourceTrigger=PropertyChanged}" Width="220" Padding="4" />
        </StackPanel>

        <TextBlock Grid.Row="4" Text="{Binding ResultMessage}" TextWrapping="Wrap" VerticalAlignment="Top">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Foreground" Value="{StaticResource AccentBrush}" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSuccess}" Value="False">
                            <Setter Property="Foreground" Value="{StaticResource DangerBrush}" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" Command="{Binding CloseCommand}" Margin="0,0,8,0" Background="Gray" />
            <Button Content="Confirm Booking" Command="{Binding ConfirmCommand}" Padding="16,8" />
        </StackPanel>
    </Grid>
</Window>
